### *** [x] ** [x] **2. 实现分割面板UI**:
    * **任务**: 在 "分割工具" `QGroupBox` 中，添加 "开始主动脉根部分割"、"开始瓣膜支架分割" 和 "验证分割结果" 三个 `QPushButton`。
    * **验证**: 界面上应能看到这三个按钮。
    * **✅ 已完成**: 2025-08-06 - 三个分割按钮已成功创建，使用正确的shadcn/ui样式 搭建UI基础布局**:
    * **任务**: 在 `module2/module2_widget.py` 中，创建 "分割工具 (Segmentation)" 和 "解剖标志点定义 (Landmark Placement)" 两个 `QGroupBox`。
    * **验证**: 运行Slicer，切换到模块二，确认能看到这两个带标题的分组框。
    * **✅ 已完成**: 2025-08-06 - UI基础布局已成功实现，两个分组框正确显示R Analytics - 模块二功能开发计划 (Todo List)**

#### **第一阶段：UI骨架与核心交互 **

*目标：搭建用户能看到并与之交互的基础界面框架，验证UI布局和组件的响应。*

* [ ] **1. 搭建UI基础布局**:
    * **任务**: 在 `module2/module2_widget.py` 中，创建 “分割工具 (Segmentation)” 和 “解剖标志点定义 (Landmark Placement)” 两个 `QGroupBox`。
    * **验证**: 运行Slicer，切换到模块二，确认能看到这两个带标题的分组框。

* [ ] **2. 实现分割面板UI**:
    * **任务**: 在 “分割工具” `QGroupBox` 中，添加 “开始主动脉根部分割”、“开始瓣膜支架分割” 和 “验证分割结果” 三个 `QPushButton`。
    * **验证**: 界面上应能看到这三个按钮。

* [ ] **3. 实现标志点定义面板UI**:
    * **任务**: 在 “解剖标志点定义” `QGroupBox` 中，添加 “定义原生瓣环”、“定义原生连合”、“定义新连合”、“定义冠脉开口” 等核心 `QPushButton`。
    * **验证**: 界面上应能看到这些标志点定义按钮。

* [ ] **4. 实现进度与控制面板UI**:
    * **任务**: 添加 “进度状态” 和 “操作” 两个 `QGroupBox`，并放入 `QProgressBar`、`QLabel` (用于状态文本)、“重置” 和 “完成模块” 按钮。
    * **验证**: 界面上应能看到进度条、状态文本和两个操作按钮。

* [ ] **5. 连接按钮信号**:
    * **任务**: 为所有新创建的按钮连接 `clicked` 信号到一个临时的槽函数（例如 `_on_button_clicked(self, button_name)`）。在该函数中，只需用 `logging.info(f"{button_name} clicked")` 打印日志即可。
    * **验证**: 点击每个按钮，Slicer的日志中应能看到对应的输出，确认信号连接无误。

#### **第二阶段：时相自动设定与数据承接 **

*目标：实现模块二的核心工作流入口逻辑，确保能正确接收模块一的数据。*

* [ ] **6. 实现自动时相切换**:
    * **任务**: 在 `Module2Widget` 的 `on_activated` 方法中（或者在模块被切换到的时机），实现自动切换到舒张末期的逻辑。
        1.  从 `self.session.get_marked_phase('end_diastole')` 获取帧索引。
        2.  如果获取成功，通过 `slicer.mrmlScene` 找到 `vtkMRMLSequenceBrowserNode`。
        3.  调用其 `SetSelectedItemNumber()` 方法跳转到指定帧。
    * **验证**: 完成模块一并标记时相后，点击切换到模块二，Slicer的视图应自动跳转到您标记的舒张末期图像。

#### **第三阶段：核心功能 - TAVR支架分割 (预计2-3天)**

*目标：完成第一个核心业务功能，实现用户价值闭环。*

* [ ] **7. 逻辑层 - 实现支架分割**:
    * **任务**: 在 `module2/module2_logic.py` 中，创建一个 `segment_stent_with_threshold` 方法。
        1.  该方法应能程序化地调用Slicer的`SegmentEditorWidget`。
        2.  自动选择 “Threshold” (阈值) 效果。
        3.  根据设计文档，设置一个推荐的HU值范围（例如 800-3000 HU）。
    * **验证**: 在Python交互控制台手动调用此方法，能看到Segment Editor被激活并应用了阈值。

* [ ] **8. 界面层 - 连接支架分割按钮**:
    * **任务**: 将 “开始瓣膜支架分割” 按钮的点击事件连接到 `module2_logic.py` 的新方法上。
    * **验证**: 在插件UI上点击该按钮，应能触发支架分割流程。

* [ ] **9. 创建并管理分割节点**:
    * **任务**: 在 `Module2Logic` 中实现逻辑，确保在首次分割时，创建一个名为 `TAVR_Segmentation` 的 `vtkMRMLSegmentationNode`，并在其中创建一个名为 `Stent` 的segment。
    * **验证**: 分割完成后，在Slicer的Data模块中能看到正确的分割节点和segment。

#### **第四阶段：核心功能 - 主动脉根部分割**

*目标：完成第二个核心业务功能，并处理更复杂的交互。*

* [ ] **10. 逻辑层 - 实现主动脉分割**:
    * **任务**: 在 `module2_logic.py` 中，创建 `segment_aorta_with_seeds` 方法。
        1.  该方法能激活Segment Editor并选择 "Grow from Seeds" (种子点增长) 效果。
        2.  提供切换到 "Aorta" 和 "Aorta_Background_Seeds" 两个不同segment进行绘制的功能。
    * **验证**: 通过Python控制台调用，能够成功切换画笔工具和目标segment。

* [ ] **11. 界面层 - 连接主动脉分割系列按钮**:
    * **任务**: 将 “放置主动脉种子点”、“放置背景种子点” 和 “运行种子点增长分割” 按钮分别连接到 `Module2Logic` 中对应的控制方法上。
    * **验证**: 在UI上依次点击这些按钮，能够引导用户完成整个种子点分割流程。

#### **第五阶段：核心功能 - 原生瓣环平面重建 **

*目标：完成第一个关键几何计算功能，为后续测量打下基础。*

* [ ] **12. 逻辑层 - 实现平面重建算法**:
    * **任务**: 在 `module2_logic.py` 中，创建一个 `reconstruct_native_annulus_plane` 方法。
        1.  该方法能自动创建并激活一个名为 `Native_Annulus_Points` 的 `vtkMRMLMarkupsFiducialNode`。
        2.  在用户放置了3个点后，获取这些点的三维坐标。
        3.  使用 VTK 库（如 `vtkPlane.ComputeBestFittingPlane`）计算最佳拟合平面。
        4.  将计算出的平面对象（`vtkPlane`）存储到 `TAVRStudySession` 中。
    * **验证**: 在场景中放置3个点，手动调用此方法，能正确计算平面并在会话中存储。可以在Python控制台打印平面信息验证。

* [ ] **13. 界面层 - 连接并引导用户**:
    * **任务**: 将 “定义原生瓣环” 按钮连接到上述逻辑方法。同时，在UI上添加一个 提示，用于实时提示用户当前的操作。
    * **验证**: 点击按钮，UI出现提示，用户可以顺利完成三点放置并触发平面计算。

---

**后续阶段建议**:

* **第六阶段**: 实现瓣膜特异性平面重建的**策略模式框架**。
* **第七阶段**: 逐一实现**每个瓣膜型号**的平面重建策略子类。
* **第八阶段**: 逐一实现**各个标志点**的放置与存储逻辑。
* **第九阶段**: 实现完整的**错误处理与边缘情况**。
* **最终阶段**: 完善**模块API**并进行整体测试。

这个计划将一个复杂的大任务分解成了多个可管理、可验证的小块。建议您严格按照顺序执行，每完成一步都进行充分的测试，这样可以确保整个开发过程稳健、高效。